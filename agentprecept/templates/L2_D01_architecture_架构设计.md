# 架构设计

> 分类: D | 层级: L2 | 编号: L2_D01
> 状态: ⏳ 待撰写 | 目标读者: 设计审查

---

## AGENT: 填充流程

```
1. 读取 docs/project-graph.yaml 的 structure 层
2. 用 tree src/ -I '__pycache__|test|node_modules' -L 3 生成目录树
3. 按职责分组: api/routes → 路由层, services → 业务层, models → 数据层
4. 读取 relations 层 → 标注组间数据流方向
5. 输出 ASCII 缩进架构图（不要求图片——纯文本）
6. 填充 §2 模块表
```

---

## §1 系统架构图

<!-- AGENT: 执行上方流程第1-5步后在此插入 ASCII 图 -->

```
[在此插入 ASCII 架构图]
```

## §2 模块划分

<!-- AGENT: 从 project-graph 提取所有模块 → 按职责分组 -->

| 模块 | 职责 | 依赖 |
|------|------|------|
| _示例_ | _描述_ | _依赖哪些模块_ |

## §3 技术选型

| 组件 | 选择 | 原因 |
|------|------|------|
| _示例_ | _名称_ | _见 L4_O01_ |

## §4 关键设计决策

> 详见 `L4_O01` 设计依据

## 完成标准

□ 架构图包含 ≥ 3 个模块
□ 每个模块有职责描述
□ L4_O01 有对应的技术选型依据
□ 已运行 `graph-to-mermaid.py` 生成 Mermaid 视图

## 反模式

| 不要 | 要 |
|------|------|
| 只放一个框"系统" | 按 api/services/models 分组，至少 3 层 |
| 用外部工具画图后截图粘贴 | 用 Mermaid 代码块——Agent 可维护 
